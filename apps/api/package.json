{
  "name": "@certificate-platform/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../../.env --import tsx src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@certificate-platform/auth": "workspace:*",
    "@certificate-platform/config": "workspace:*",
    "@certificate-platform/contracts": "workspace:*",
    "@certificate-platform/database": "workspace:*",
    "@certificate-platform/domain": "workspace:*",
    "@certificate-platform/queue": "workspace:*",
    "fastify": "5.12.0",
    "pino": "10.3.1",
    "ioredis": "6.0.0",
    "zod": "4.4.3"
  }
}
